# Hub 任务轮询 Automation 记忆

## 2026-04-17

**执行结果：** Hub 在线，无 pending 任务，正常结束。

**重要发现：** 用户查询指令中指定的 API 端点有误：
- ❌ `GET /api/agents/workbuddy/tasks?status=pending` → 404
- ❌ `GET /api/agents/workbuddy/messages?status=unread` → 404

实际正确端点（参考 `scripts/hub_watcher.py`）：
- ✅ `GET /api/tasks?agent_id=workbuddy&status=pending` → 返回 `{"tasks":[],"count":0}`
- 消息通过 SSE `/events/{agent_id}` 推送，无独立 REST 查询端点

**建议：** 更新自动化任务中的 API 端点为正确路径。

## 12:11 Hub 轮询执行记录

- Hub 健康检查：通过（uptime 6618s）
- API 路由需要 `agent_id=workbuddy` 参数（之前忘记传，返回 `agent_id is required`）
- 待处理任务：0 条
- 未读消息：0 条
- 结果：无操作，静默退出
